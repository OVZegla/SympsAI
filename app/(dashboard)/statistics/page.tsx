import { PhasePlaceholder } from "@/components/ui/PhasePlaceholder";

export default function StatisticsPage() {
  return (
    <PhasePlaceholder title="Statistiques" phase="Phase 8 — à venir">
      <p>
        Les statistiques (incidents par machine, par composant, causes
        fréquentes, taux de récurrence — spec §45) arrivent en phase 8, une fois
        que suffisamment d&apos;incidents auront été enregistrés.
      </p>
    </PhasePlaceholder>
  );
}
